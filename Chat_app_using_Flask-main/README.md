# Chat_app_using_Flask

To run this project first make shore you downloaded the Flask package.
If not use "pip install Flask" command.

Then run the main.py file.
If the styles are not applied then change the path of style.css file in home.html file.
